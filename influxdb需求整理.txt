1.针对influxdb,请写出来以下几个需求的demo:
1.1 插入一条时序数据， 数据包含信息为：设备信息，数据采集点meta信息，数据采集点的值，采集时间戳;
1.2 读取指定设备，指定采集点，指定时间戳的值;
1.3 读取指定设备，指定采集点，制定时间范围内的值;
1.4 读取指定设备，指定采集点，指定时间范围内的最大值;
1.5 更新指定设备，指定采集点，指定时间戳的值;
1.6 删除指定设备，指定采集点，指定时间戳之前的所有值;
1.7 导入一份数据(不用太多，10条即可);
2,请把上述的1.1,1.2,1.3,1.4,1.5,1.6,以java实现
3,在2017-06-11时能交付一个格式自己定义的版本，功能都要自测，交付后我会给一个标准的接口，然后在接口上实现以上功能
4,对需求有任何疑问请及时沟通