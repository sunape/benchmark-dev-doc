1.mysql支持时序数据的存储和读取吗?
2.mysql如果支持，需要对mysql做什么样的配置，才可以正确的存储和读取时序数据?
3.如果mysql支持时序数据存储，请写出来以下几个需求的demo:
3.1 插入一条时序数据， 数据包含信息为：设备信息，数据采集点meta信息，数据采集点的值，采集时间戳;
3.2 读取指定设备，指定采集点，指定时间戳的值;
3.3 读取指定设备，指定采集点，制定时间范围内的值;
3.4 读取指定设备，指定采集点，指定时间范围内的最大值;
3.5 更新指定设备，指定采集点，指定时间戳的值;
3.6 删除指定设备，指定采集点，指定时间戳之前的所有值;
3.7 导入一份数据(不用太多，10条即可);
4.以上需求做完后，请把结果以及参考过的文章发我一下。